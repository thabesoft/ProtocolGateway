<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             
             xmlns:vm="using:ThabeSoft.ProtocolGateway.ViewModels"
             xmlns:page_vm="using:ThabeSoft.ProtocolGateway.ViewModels.Pages"
             
             x:Class="ThabeSoft.ProtocolGateway.Views.Pages.ChannelPage"

             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:DataType="page_vm:ChannelPageViewModel">

    <UserControl.DataContext>
        <page_vm:ChannelPageViewModel />
    </UserControl.DataContext>

    <Grid RowDefinitions="Auto,*">

        <!--顶部信息-->
        <StackPanel Grid.Row="0" Orientation="Vertical">

            <!--信息-->
            <UniformGrid Height="50" Columns="2">

                <TextBlock VerticalAlignment="Center" Text="测试" />
                <TextBlock VerticalAlignment="Center" Text="测试" />

            </UniformGrid>

            <!--工具栏-->
            <DockPanel>

                <TextBox DockPanel.Dock="Left" Text="HelloWorld!" />
                <Button Margin="10 0" DockPanel.Dock="Left" Content="刷新"
                        Command="{Binding ReloadCommand}"/>

            </DockPanel>

        </StackPanel>

        <DataGrid Margin="0 10 0 0" Grid.Row="1"
                  ItemsSource="{Binding Channels}"
                  AutoGenerateColumns="False"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True">

            <DataGrid.Columns>

                <DataGridTemplateColumn Header="名称" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <HyperlinkButton Content="{Binding Name}"
                                             Command="{Binding $parent[DataGrid].DataContext.OpenDetailsPageCommand}"
                                             CommandParameter="{Binding}"
                                             Classes="TextButton"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="协议" Binding="{Binding Protocol}" Width="*" />

                <DataGridTemplateColumn Header="操作" Width="Auto">

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="连接"
                                        IsVisible="{Binding CanConnect}"
                                        Command="{Binding ConnectCommand}"
                                        Classes="DataGridButton"/>
                                <Button Content="断开"
                                        IsVisible="{Binding CanDisconnect}"
                                        Command="{Binding DisconnectCommand}"
                                        Classes="DataGridButton"/>
                                <Button Content="暂停"
                                        Command="{Binding PauseCommand}"
                                        ToolTip.Tip="启动"/>
                                <Button Content="恢复"
                                        Command="{Binding ResumeCommand}"
                                        ToolTip.Tip="停止"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>
